// TODO: mover isso para uma variável de ambiente antes de ir para produção
const db_password = "MPassword12345";

function connectToDatabase() {
  console.log('Conectando com a senha: ' + db_password);
}